const config = {
    home: '*',
    search: '/search',
    detail: '/products/:id',
    cart: '/cart',
    checkout: '/checkout',
    shops: '/all-shops',
    shop: '/shop/:id',
    sale: '/sale-page/*',
    user: '/user/*',
};

export default config;
